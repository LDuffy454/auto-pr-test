name: Auto-PR Robot
on: [push]
jobs:
  generate-update:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4

      - name: Read notes
        id: read
        run: |
          echo "NOTES<<EOF" >> $GITHUB_OUTPUT
          cat notes.txt >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Make Marketing Magic
        run: |
          echo "# 🚀 Fresh Update Dropped!" > update.md
          echo "" >> update.md
          echo "Hey friends! We just shipped some fresh updates:" >> update.md
          echo "" >> update.md
          cat notes.txt | sed 's/^/✨ /' >> update.md
          echo "" >> update.md
          echo "Love it? [Try now](https://your-app.com) · Follow for more!" >> update.md
          echo "" >> update.md
          echo "_Auto-powered by **Auto-PR** 🤖_" >> update.md

      - name: Save File
        uses: actions/upload-artifact@v3
        with:
          name: Sexy Update
          path: update.md
